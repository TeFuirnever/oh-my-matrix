{
  "name": "@oh-my-matrix/instinct",
  "version": "0.1.0",
  "description": "Cross-session context memory: observes tool calls (scrubbed) and recalls recent observations at session start, closing the autopilot context-memory gap.",
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/TeFuirnever/oh-my-matrix.git", "directory": "packages/instinct" },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
