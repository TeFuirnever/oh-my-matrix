{
  "id": "dynamic-workflows",
  "name": "Dynamic Workflows Guard",
  "description": "Runtime subagent guard (before_tool_call priority 11, fail-closed destructive-op blocking for :subagent: sessions) + orchestration skill (.prose generation, 11 patterns, role-prompt templates) + deterministic task prescreen (agent_turn_prepare, fan-out signal nudge for main sessions). Imports permission primitives (decidePermission, classifyCommand, audit) from @oh-my-matrix/permission-policy.",
  "version": "1.2.0",
  "main": "dist/index.js",
  "activation": {
    "onStartup": true
  },
  "hooks": [
    "before_tool_call",
    "agent_turn_prepare"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "highRiskTools": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": []
      }
    }
  }
}
