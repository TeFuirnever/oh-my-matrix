{
  "name": "@oh-my-matrix/permission-policy",
  "version": "0.1.3",
  "description": "Shared permission policy primitives (decidePermission, classifyCommand, audit) consumed by @oh-my-matrix/autopilot + @oh-my-matrix/dynamic-workflows. Pure library, not a plugin.",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/TeFuirnever/oh-my-matrix.git",
    "directory": "packages/permission-policy"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "pack": "pnpm pack",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm run build"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
