{
  "name": "@openclaw/permission-policy",
  "version": "0.1.0",
  "description": "Shared permission policy primitives (decidePermission, classifyCommand, audit) consumed by @openclaw/autopilot + @openclaw/dynamic-workflows. Pure library, not a plugin.",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "pack": "pnpm pack"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
