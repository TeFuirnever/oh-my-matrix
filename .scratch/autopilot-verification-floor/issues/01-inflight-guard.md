# 01 — §5.6 在飞守卫（前置）

**What to build:** 长工具调用（含验证命令）不再被停滞检测误报。工具派发后到结果到达前，停滞计时被抑制；验证命令执行期间同样抑制（消除 TOCTOU 覆写）；turn 结束或崩溃后字段清零，防止永久禁用停滞检测。

**Blocked by:** None — can start immediately

**Status:** ✅ 已实施（2026-08-09 代码核验：`index.ts:268` validation 前置置位、`:811` evidence 期置位、`:912/920/1293` 清零）

- [x] 验证命令耗时超过停滞阈值时不触发误报（run 完成态不再被巡检的 retry_queued 覆写）
- [ ] 工具派发后、tool_result 到达前，静默计时使用单工具上限而非停滞阈值
- [ ] turn 结束（正常/崩溃）字段清零，不残留悬挂
- [ ] 既有停滞检测契约测试回归绿
